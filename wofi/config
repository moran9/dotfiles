width=560
height=420
location=center
show=drun
prompt=Search
filter_rate=100
allow_markup=true
no_actions=true
halign=fill
orientation=vertical
content_halign=fill
insensitive=true
allow_images=true
image_size=28
gtk_dark=true
hide_scroll=true
matching=fuzzy
# Terminal for launching Terminal=true desktop entries (btop, lazygit,
# htop, ranger, etc.). Without this, wofi tries the system default and
# fails on Hyprland.
term=kitty
